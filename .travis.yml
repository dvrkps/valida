language: go

go:
    - 1.7
    - tip

notifications:
    email: 
        recipients:
            - davor.kapsa@gmail.com
        on_success: never
        on_failure: always

script:
    - ./coverage.sh

after_success:
    - bash <(curl -s https://codecov.io/bash) || echo "Codecov fails"
